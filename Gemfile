source "https://rubygems.org"


# Use GitHub Pages gem to match what GitHub uses on the server
gem "github-pages", group: :jekyll_plugins

# Enable remote themes (required for Minimal Mistakes via remote_theme)
gem "jekyll-remote-theme"

group :jekyll_plugins do
  gem "jekyll-feed"
end
